// Temporary mapping while /api/me exposes role_id rather than
// a semantic role name. Replace when the API contract is updated.

export const ROLE_IDS = {
  EMPLOYEE: 1,
  MANAGER: 2,
  ADMIN: 3,
};